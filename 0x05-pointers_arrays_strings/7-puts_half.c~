#include "holberton.h"
/**
 * puts_half - prints second half of a string
 * @str: sting
 * Return: void
 */
void puts_half(char *str)
{
int i = 0;
int n;
while (*(str + i) != '\0')
i++;
if (i % 2 == 0)
n = i / 2;
else
n = (i - 1) / 2;
for (i = n; i >= 0; i--)
_putchar(*(str + i));
}
